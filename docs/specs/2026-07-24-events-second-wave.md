# events second wave

> events-cli ships its second wave: the deferred arcs land on the first slice — the agentfront MCP+HTTP binding from the core registry (#6), durable identity-owned subscriptions with history and bounded cursor drain (#7), pipelines over the event graph (#8), shell-cli-routed stack verbs (#9), and dynsec identities with a documented remote-access opt-in (#10) — with issue #3 answered and closed
> instruction: This spec's build scope is the opening arc — #7 durable subscriptions + history + cursor drain, with the #3 reply/close and the pyproject description fix riding it (resolved q1) — #6/#8/#9/#10 land per the confirmed contracts on their issues afterward; verify each arc on its own issue, never as a wave-level judgment call

## Audience

- Consumers of the contract lane who need bounded, resumable consumption: agents draining events inside a turn (the MCP/HTTP shape #6 will derive later), humans and scripts on the events CLI, and services importing events_cli — first on spark-f8a9. Producer-owned raw trees (reachy/*) keep raw MQTT and are unaffected

## Before → After

- Before: Today the fabric is publish-only: EventClient publishes, the reTerminal bridge subscribes over raw MQTT, and there is no subscribe/drain/history surface at all — 'events watch' does not exist, a contract-lane consumer that restarts loses everything published while it was away, and no store holds event history
- After: events-cli has a consume side: named durable subscriptions survive broker and consumer restarts; 'events watch <sub> --since <cursor> --max N --timeout S' returns a bounded batch plus a cursor with nothing acknowledged lost; 'events emit/get/list' round-trip envelope events through the contract lane; drained history lives in a store that survives stack restarts — #1's emit-and-watch acceptance items are met for the event plane

## Requirements

- Issue #3's two outstanding asks get an events-cli reply and the issue closes: the importable symbol is 'from events_cli import EventClient' (lazy PEP 562 re-export; events_cli.client defines it — their placeholder guess 'EventsClient' is one letter wrong and would degrade to a silent no-op), the constructor is EventClient(host='127.0.0.1', port=1883, *, client_id=..., connect=True) so connect() runs in the constructor by default, and the reTerminal bridge subscribes over raw loopback MQTT for this wave (client-side drain arrives with #7); acceptance is already recorded 10/10 in docs/acceptance/2026-07-24-issue-3-live-run.md
  - honesty: The reply posted on #3 names EventClient, its import path, the constructor signature, connect-in-constructor, and the raw-MQTT subscribe answer for the reTerminal bridge; the issue is closed after the reply (or explicitly left open by the operator with the reason stated)
- Issue #9's reopen trigger has fired: shell-cli 0.13.x ships process.exec/process.shell behind a policy gate with persisted evidence records (its README Status section still claims no runner exists — stale against its own CHANGELOG), so the #9 evaluation of routing events up/down/logs/status through shell-cli is actionable now; any migration must keep StackError->CliError, exit code 2 for environment errors, and no traceback on stderr
  - honesty: The #9 evaluation runs during this wave and its verdict — migrate or stay on subprocess, with reasons — is recorded on issue #9, not silently dropped
- Issue #7's cursor design must not assume total order on envelope ids: evt_ ids are Crockford ULIDs sortable at millisecond granularity only, with no intra-millisecond monotonicity (recorded on the events-first-slice frame) — the cursor must be store-assigned sequence, or the core must grow monotonic ids, before replay-from-cursor lands
  - honesty: A test mints two envelope ids in the same millisecond and proves drain order equals store append order and cursor resume is exact — no ordering is ever derived from comparing ULIDs
- Issue #10's dynsec landing removes 'allow_anonymous true' from the generated mosquitto.conf — the template's own comment binds them ('when they land, this line goes away') — while keeping the anonymous co-located loopback path #3 depends on (REACHY_MQTT_URL=localhost:1883), and a stack-marked test must verify the dynsec plugin actually ships and initialises in eclipse-mosquitto:2.1.2-alpine (per_listener_settings is false, so auth settings are global)
  - honesty: Deferred-arc condition: when #10 lands, the regenerated template drops allow_anonymous true, a stack-marked test proves the dynsec path in eclipse-mosquitto:2.1.2-alpine, and the anonymous co-located loopback lane #3 depends on still works; until then this wave keeps anonymous loopback
- The pyproject [project] description drift is fixed in the wave's first PR: it presently claims events-cli 'fronts it through agentfront as a CLI, an HTTP API and an MCP surface' in the present tense while #6 is unbuilt — prose ahead of code, exactly what CLAUDE.md's Known-drift rule exists for; reword to today's truth (or record in Known drift until #6 lands)
  - honesty: The pyproject description on main no longer claims MCP/HTTP surfaces exist; the rewording rides the arc's first PR with its version bump
- Durable subscriptions are MQTT persistent sessions, not a resident control service: 'events sub add <name> <pattern>' registers a broker-side persistent session (clean_start=false, QoS 1, session expiry effectively infinite) subscribed to the pattern's topic filter, so the broker's existing persistence (persistence true + volume + autosave 60) buffers the backlog while no drainer is connected — no always-on control-service container ships in this arc (the resident service becomes #8's concern when pipelines need active run advancement); the undrained-backlog bound (max_queued_messages) is set explicitly in the generated mosquitto.conf and documented
  - honesty: A stack-marked test proves the architecture: sub add, publish N envelopes with no drainer connected, docker restart of the broker, then drain returns all N in order — and the generated mosquitto.conf sets max_queued_messages explicitly with the bound documented in docs/contract.md
- 'events watch <sub> --since <cursor> --max N --timeout S' drains: resume the session, consume up to --max, append to the history store, acknowledge, return the batch plus the next cursor; defaults are non-infinite (--max 100, --timeout 30) and there is no unbounded --follow in this arc
  - honesty: A drain call returns within its --timeout with at most --max events plus a cursor; resuming from the returned cursor re-delivers nothing acknowledged; drain/cursor logic is tested pure and dockerless, the broker path behind the stack marker
- 'events emit <type> --data <file|->' validates through the core envelope (generated id/time) and publishes QoS 1 to the canonical topic via EventClient; 'events get <event-id>' and 'events list --type <t> --max N' read drained history from the store — every verb with --json, registered via register(sub) with catalog entries
  - honesty: Two CLI processes demonstrate the emit-to-watch round-trip on a live stack (stack-marked); get/list return drained envelopes by id and type with --json; envelope validation and store logic keep their coverage in the default dockerless selection
- The canonical transport mapping is defined and documented in docs/contract.md: envelope events publish to 'events/<type with dots as topic-level separators>' and a subscription pattern like 'task.*' compiles to the matching MQTT filter — topic names stay a transport detail per #1, the dotted type stays the contract, and producer-owned trees stay outside the mapping
  - honesty: The type-to-topic and pattern-to-filter mapping is pure and tested dockerless in both directions; docs/contract.md documents it; and a test proves reachy/* topics are not captured by contract-lane subscription patterns
- Every subscription records an owner identity as a declared name (default resolved from culture.yaml nick, falling back to the client id) — unauthenticated on loopback in this arc, forward-compatible with #10's dynsec so identity-owned subscriptions gain real authentication without a schema change
  - honesty: Every subscription record carries its owner; 'events sub list --json' shows it; and the record schema needs no migration when #10 lands (the dynsec identity name is the owner name)
- The stack-marked integration suite runs against an isolated scratch stack — its own compose project/container name, host port, volume and stack dir (EVENTS_STACK_DIR override) — and never restarts the production events-mosquitto, which serves the robot's nervous system live on this box; applying the template change (max_queued_messages) to the spark-f8a9 broker is a documented service window with rollback, like the first slice's cutover
  - honesty: The integration suite's setup provably targets its own compose project name, port and volume (asserted in the tests themselves), events-mosquitto's uptime is unbroken by a full suite run, and the live template rollout is recorded as a service window with a rehearsed rollback in docs/acceptance/
- Backlog overflow semantics are documented from evidence, not assumption: mosquitto 2.1.2's default bound is 1000 queued messages per offline session and overflow drops the NEWEST messages silently (probe: 1200 published offline, exactly m00000-m00999 delivered in order; one broker log notice) — the template sets max_queued_messages explicitly, contract.md documents drop-newest, and the watch/drain docs point at 'events logs' for the drop notice
  - honesty: A stack test publishes bound+K messages to an offline subscription and proves exactly 'bound' oldest messages arrive in order; the template carries max_queued_messages with a comment stating drop-newest; contract.md documents it
- Concurrent drains of one subscription are MQTT session takeover (the broker kicks the first drainer; observed and logged by the probe): persist-then-ack plus store dedupe on event id makes takeover lose nothing, and contract.md documents single-drainer-per-subscription semantics
  - honesty: A stack test starts drain A, starts drain B on the same subscription, and proves the store holds every event exactly once afterwards; contract.md documents single-drainer semantics
- Durable capture requires QoS 1 publishes and the docs must say so: EventClient.publish AND publish_event default to qos=0, so an importable-client producer publishing envelopes with defaults silently bypasses durable capture — 'events emit' publishes qos=1 explicitly, and the client docstrings + contract.md state that QoS 0 bypasses capture
  - honesty: 'events emit' passes qos=1 explicitly (unit-asserted); client.py docstrings and contract.md both state that QoS 0 publishes bypass durable capture
- The history store is per-host like the stack itself: it lives beside the stack state (default_stack_dir's XDG convention, honouring an env override), never CWD-relative — 'events list' answers the same from any directory — and every stored record carries a store-format version marker so a future migration has something to key on
  - honesty: 'events list' returns identical results from two different CWDs against the same host stack (test); stored records carry the format-version field from the first write
- Subscription names and patterns are validated at the boundary: names are slugs, patterns use the dotted-type grammar with '*' only, and raw MQTT filter characters (#, +, /) are rejected with a field-level error — a pattern can never escape the events/ topic prefix
  - honesty: Unit tests reject '#', 'a/+', 'a/b', '../x' and empty patterns with field-level errors, dockerless; the compiled filter is always prefixed events/

## Honesty conditions

- The wave announcement is honest only arc-by-arc: the exported spec marks #6/#8/#9/#10 as deferred with issue links and claims nothing delivered beyond the opening arc — mirroring how the first-slice spec carried its own deferred rows
- The rubric gate (agentfront cli doctor --strict) and the pinned contract tests — dual catalog root keys, prog resolution, stdout/stderr split, exit codes — pass unchanged on every PR of the arc; enforcement is CI, not intention
- Each landing PR touches docs/contract.md's built/not-built table in the same change that adds the surface — a PR adding a verb without the table update does not merge
- The named consumers are real: the drain shape serves an agent turn within bounded time (proven by the --timeout tests), and reachy's raw-MQTT lane is untouched by the arc's diff
- Every element of the after-state is mechanically checkable via the stack-marked suite: survive-restart, bounded drain, cursor resume, and the emit-to-watch round-trip — nothing rests on a judgment call
- The before-state is evidence, not narrative: at the pre-arc HEAD, events_cli contains no subscribe/consume/store code and no watch/emit/get/list/sub verb exists in the parser or the explain catalog
- The success signal runs as an executable script or marked test on spark-f8a9 with observable output per item, exiting non-zero on any failure — a gate, not a demo, like scripts/acceptance-issue-3.sh
- The arc's PRs add no agentfront-binding, pipeline, dynsec or SSE code; the deferred issues stay open with their contracts intact; the arc's diff contains no surface from #6/#8/#10
- docs/contract.md states the capture boundary in its own section, and a stack test proves an event published before 'sub add' is absent from a later drain while one published after is present

## Success signals

- Stack-marked proof on spark-f8a9: create a subscription, publish N envelopes while no drainer is connected, restart the broker, then drain — all N arrive in order within --timeout with a cursor; resuming from that cursor returns nothing already acknowledged; a second CLI process watches what the first emits; the default pytest selection stays dockerless throughout

## Scope / boundaries

- The stable CLI contract survives every arc of the wave: _output.py stdout/stderr split and _errors.py exit codes stay frozen, the explain catalog keeps BOTH ('events',) and ('events-cli',) root keys, no traceback ever reaches stderr, and every new verb (watch, pipeline, dynsec-facing) goes through register(sub) plus a catalog entry with non-infinite --max/--timeout defaults — the rubric gate and pinned tests are the enforcement
- docs/contract.md's 'What is built, and what is not' table is updated in the same PR as each arc that lands — it is currently honest (watch/pipeline verbs absent, dynsec listed 'not built — #10'), and keeping it honest is part of each arc's definition of done, not a trailing docs task
- This arc delivers #7 plus the riders (the #3 reply/close and the pyproject description fix) only: no agentfront MCP/HTTP (#6), no pipelines (#8), no shell-cli migration (#9 — its evaluation verdict is recorded on the issue separately), no dynsec (#10), no SSE/websocket streaming — each deferred arc lands per its own issue contract, mirroring the first-slice precedent
- History captures only what a registered subscription's persistent session queued: events emitted before any subscription exists, or published at QoS 0, are transported but never captured — there is no global capture until #8's control service; docs/contract.md states this so no consumer assumes 'events list' is a complete log

## Non-goals

- Issue #1's non-goals hold across the whole wave: no reimplementing MQTT, no exactly-once guarantees, no arbitrary code execution inside the events service, no bespoke history store before the eidetic-cli/data-refinery-cli evaluation adjudicates, and producer-owned topic trees (reachy/events/{source}/{type}, retained reachy/state/{key}) stay exempt from envelope validation and pipelines

## Assumptions

- Issue #7's store evaluation has a shipped concrete candidate: data-refinery-cli wave 2 provides 'import data_refinery.store' (Envelope, put/get/list/migrate; files backend dependency-free, mongo/neo4j behind the [store] extra), with eidetic-cli consuming it for the memory lane — the no-bespoke-store constraint is now an evaluation against real APIs, not a hypothetical
- Issue #6's toolchain is already in place: agentfront 0.20.0 is the dev dep, agentfront.testing.assert_surfaces_agree imports cleanly, and App exposes tool/add_command/group/http_app/mcp_server/registry — the remaining work is exactly the confirmed c6/h6 contract: populate the registry from events_cli/core (never separate adapters), derive MCP behind [mcp] + HTTP, pin CLI/registry parity with our own tests, non-infinite --max/--timeout on every tool
- The wave's dependency order is partially forced: #8 requires #7 (pipelines advance runs by consuming correlated events over durable history — #8's own scope says that arc lands first); #7's identity-owned subscriptions fire #10's recorded reopen trigger, so dynsec design rides with or immediately after #7; #6 and #9 are independent of both and of each other

## Scope exploration

- `s1` — `issues #6-#10 (confirmed spec contracts on each)`: every deferred arc carries a confirmed contract comment recorded on the issue itself (c6/h6 for the binding, c8/h8 for durable subscriptions, reopen triggers for #9/#10) — the requirements baseline for the wave lives on the issues, not in-repo, and #8's scope explicitly sequences #7 first
  - seeds: `c12`
- `s2` — `issue #3 comment thread + events_cli/__init__.py + events_cli/client.py`: three reachy-mini-cli comments, zero events-cli replies: Ask 1 (exact importable symbol + constructor + connect story) and Ask 2 (subscribe over raw loopback MQTT this slice?) are both answerable today from code already on main — EventClient via lazy PEP 562 re-export, host/port/client_id kwargs, connect=True in the constructor; their placeholder binding guesses 'EventsClient' and would silently no-op
  - seeds: `c2`
- `s3` — `shell-cli CHANGELOG 0.13.0/0.13.1 + shell/cli/_commands/{operation,policy}.py`: the runner exists: process.exec and process.shell behind the policy gate, shell policy check/explain, shell operation show, persisted evidence records with secret redaction — shell-cli's README Status section ('no operation... has been built yet') is stale against its own changelog; #9's 'once its runner exists' condition is met
  - seeds: `c3`
- `s4` — `data-refinery-cli README (wave table) + import data_refinery.store`: wave 2 shipped the importable store API: Envelope/put/get/list/migrate, files backend dependency-free, mongo+neo4j behind [store], eidetic-cli consumes it over a subprocess boundary — #7's evaluate-before-building clause now has a real API to evaluate
  - seeds: `c4`
- `s5` — `eidetic recall --scope events-cli (first-slice frame memories)`: prior decisions recalled, not re-derived: paho base dep (q1), hand-rolled CLI stays with MCP+HTTP-only derivation (q2), pipelines deferred (q3/q4), dynsec parked as v1 nonblocking; plus the envelope gotcha flagged for #7 — evt_ ULIDs sort at millisecond granularity only, no intra-ms monotonicity
  - seeds: `c5`
- `s6` — `events_cli/stack/templates/mosquitto.conf`: allow_anonymous true is documented in the template as temporary ('when they land, this line goes away' pointing at dynsec); the explicit listener replaces the 2.1.2 default listener set (no 9883 dashboard, no 9001 websocket); per_listener_settings false makes auth global; the file is regenerated by events init --force so dynsec config lands via the template, not hand edits
  - seeds: `c6`
- `s7` — `agentfront 0.20.0 installed via dev dep (App API + agentfront.testing probe)`: assert_surfaces_agree imports cleanly; App exposes tool/add_command/group/http_app/mcp_server/registry — the teken->agentfront migration is done, so #6's prerequisite is already on main and the binding work is registry population + parity tests, not toolchain setup
  - seeds: `c7`
- `s8` — `pyproject.toml [project] description`: the description claims 'fronts it through agentfront as a CLI, an HTTP API and an MCP surface' in the present tense — HTTP/MCP are #6's unbuilt arc, so this is prose ahead of code on the package's own PyPI face
  - seeds: `c8`
- `s9` — `docs/contract.md (built/not-built table + surfaces + delivery semantics)`: the consumer-facing contract already states what each arc must preserve: the built/not-built table names #10 as not built, no emit/watch/pipeline verbs exist, the raw MQTT port is first-class, dedupe-on-id and retained-is-not-history are stated as requirements — each landing arc updates this file in the same PR or the contract lies
  - seeds: `c9`, `c10`
- `s10` — `docs/specs/2026-07-23-events-first-slice.md (Decisions + deferred requirement rows)`: the exported spec carries the deferred arcs as requirement rows with instruction+honesty conditions (agentfront binding, watch-as-cursor-drain) and freezes the CLI contract as a boundary — the second wave inherits these rows as its baseline; #1's non-goals are restated there as holding for every arc
  - seeds: `c9`, `c11`
- `s11` — `challenge pass / cheap-probe: scratch mosquitto 2.1.2-alpine on 127.0.0.1:18831 with the repo template`: persistent session (clean_start=false, MQTT5 session-expiry 0xFFFFFFFF) reports session_present=true after a broker restart with zero extra broker config — the architecture's core premise holds; default queue bound is exactly 1000 with silent drop-NEWEST overflow, delivery in order, one broker log notice; probe container removed after
  - seeds: `c25`
- `s12` — `challenge pass / concurrency lens: probe broker log (session takeover on same-client-id reconnect)`: mosquitto logs 'disconnected: session taken over' — concurrent drains of one subscription kick the first drainer; persist-then-ack + id dedupe makes it lose nothing but the semantics must be documented and pinned by a test
  - seeds: `c26`
- `s13` — `challenge pass / hidden-dependencies lens: events_cli/client.py publish + publish_event signatures`: both default qos=0 (drop-don't-block, right for reachy) — but QoS 0 is never queued for offline sessions, so envelope publishers using defaults silently bypass durable capture; emit must pass qos=1 and the trap must be documented; whether publish_event's default flips is q3
  - seeds: `c27`
- `s14` — `challenge pass / operations lens: docker ps on spark-f8a9 + docs/acceptance/2026-07-24-issue-3-live-run.md`: events-mosquitto serves the robot's nervous system in production on this box right now; the first slice ran cutovers as timed service windows with a rehearsed rollback — the integration suite and the template rollout must follow the same discipline, which the spec previously never said
  - seeds: `c23`
- `s15` — `challenge pass / lifecycle lens: events_cli/stack/__init__.py default_stack_dir`: the stack is per-host by design (XDG config + EVENTS_STACK_DIR override, 'one broker per host is the deployment model') — the spec never said where the history store lives; a CWD-relative store would make 'events list' answer differently per directory, so the store must follow the stack's per-host convention
  - seeds: `c28`
- `s16` — `challenge pass / unstated-assumptions lens: MQTT session/QoS semantics vs the spec's history claims`: the spec reads as if history is a property of the fabric; it is a property of REGISTERED subscriptions — pre-registration and QoS 0 events are never captured, and no global capture exists until #8's control service; stated now as a boundary so consumers cannot assume a complete log
  - seeds: `c24`
- `s17` — `challenge pass / cheap-probe: paho v2 API introspection (manual_ack, clean_start, MQTT5 properties)`: paho 2.x exposes manual_ack at the constructor plus manual_ack_set/ack, clean_start on connect, and MQTT5 CONNECT properties — plan risk r2 settles positively; the persist-then-ack drain design is implementable as specified
- `s18` — `challenge pass / security lens: sub add input surface (name -> client id, pattern -> topic filter)`: user-supplied names/patterns become MQTT client ids and topic filters — unvalidated, a pattern containing #, + or / could subscribe beyond the events/ prefix; seeded the boundary-validation requirement; no other injection surface found in the arc's verbs
  - seeds: `c29`
- `s19` — `challenge pass / reversibility+observability lens: template rollout and drain-side loss visibility`: rollback for the template change exists via the previous release's events init --force plus restart (first-slice precedent); overflow loss is visible only in the broker log (one notice line) — surfaced via the c25 documentation requirement; residual: a drainer still cannot detect a gap in-band, accepted as a documented limit of this arc (control-service capture at #8 removes it)

## Open / follow-up

- A CLI streaming --follow watch mode: the CLI is the one surface that may stream forever (#2), but it is not in this arc — revisit alongside the SSE lane when #6 lands
- Durable subscriptions targeting producer-owned raw topic trees (e.g. reachy/#): outside the contract lane this arc; revisit only if a consumer asks for it
